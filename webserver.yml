---
 - name: Play to setup web server
   hosts: all
   user: nahid
   become: yes
   tasks:
        - name: latest package installed
          apt:
            name: tree
            state: latest 
...
